[project]
name = "mentoring"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.1",
]

test = [
    "pytest >= 8.4.2"
]
